# Project instruction

Before modifying this project, read `PROJECT_NOTES.md`. Every future update must include one new, genuinely distinct selectable theme while preserving the existing themes, unless the user explicitly changes that rule.
