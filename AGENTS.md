# Project instruction

Before modifying this project, read `PROJECT_NOTES.md`. Every future update must include one new, genuinely distinct selectable theme and one new, genuinely distinct selectable Character Card border while preserving all existing themes and borders, unless the user explicitly changes that rule.
